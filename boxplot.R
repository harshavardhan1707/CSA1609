# R Program to Create a Box Plot

# Input
numbers <- c(10, 20, 25, 30, 35, 40, 45, 50, 55, 80)

# Create Box Plot
boxplot(
  numbers,
  main = "Box Plot",
  xlab = "Data",
  ylab = "Values"
)